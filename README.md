# NodeHR - AI Interview System

Премиум Multi-Agent система для проведения технических интервью с архитектурой Apple-стиля и enterprise-grade качеством кода.

## 🚀 Особенности

- **Multi-Agent Architecture**: 7 специализированных агентов работают в координации
- **LangGraph State Machine**: Управление сложными workflow интервью
- **Real-time WebSocket**: Мгновенная обратная связь и обновления
- **Apple-Style UI**: Минималистичный, элегантный интерфейс
- **Structured Logging**: Детальное логирование всех действий системы
- **Final Report Generation**: Автоматическая генерация детальных отчетов с рекомендациями
- **Knowledge Gaps Analysis**: Выявление пробелов в знаниях с образовательным контентом

## 🏗️ Технологический стек

- **Backend**: Python 3.12, FastAPI, Uvicorn
- **AI/ML**: Mistral AI, LangGraph, LangChain
- **Frontend**: Vanilla JavaScript, WebSocket API
- **Architecture**: Multi-layer (API → Core → Storage)
- **Containerization**: Docker, Docker Compose

## 📋 Требования

- Python 3.12+
- Docker & Docker Compose (для production)
- Mistral AI API Key
- 2GB+ RAM (рекомендуется)

## 🛠️ Установка

### Локальная разработка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd node-hr
```

2. **Создайте виртуальное окружение:**
```bash
python3.12 -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения:**
```bash
cp env.example .env
nano .env  # или любой другой редактор
```

5. **Добавьте ваш Mistral API ключ в `.env`:**
```env
MISTRAL_API_KEY=your_mistral_api_key_here
MISTRAL_MODEL=mistral-large-latest
```

### Запуск локально

```bash
python -m app.main
```

Или через uvicorn с hot-reload:

```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

Откройте браузер: http://localhost:8000

## 🐳 Docker (рекомендуется для VPS)

### Быстрый старт

1. **Скопируйте файл с переменными окружения:**
```bash
cp env.example .env
```

2. **Отредактируйте `.env` и добавьте ваш `MISTRAL_API_KEY`:**
```bash
nano .env
```

3. **Запустите через Docker Compose:**
```bash
docker-compose up -d --build
```

4. **Проверьте логи:**
```bash
docker-compose logs -f
```

5. **Откройте браузер:** http://localhost:8000

### Управление Docker

```bash
# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Просмотр логов
docker-compose logs -f nodehr

# Пересборка после изменений
docker-compose up -d --build
```

## ⚙️ Переменные окружения

Создайте файл `.env` на основе `env.example`:

```env
# Обязательные
MISTRAL_API_KEY=your_mistral_api_key_here

# Опциональные
MISTRAL_MODEL=mistral-large-latest
APP_NAME=AI-HR
APP_ADDRESS=0.0.0.0
APP_PORT=8000
LOG_DIR=logs
MAX_VALIDATION_ATTEMPTS=3
```

## 🏛️ Архитектура

Проект следует enterprise-архитектуре с четким разделением слоев:

```
app/
├── api/                    # API слой
│   ├── endpoints/         # Роутеры (WebSocket, REST)
│   ├── schemas/           # Pydantic модели для валидации
│   ├── deps.py            # Dependency Injection
│   └── services/          # API сервисы
├── core/                   # Бизнес-логика
│   ├── engine.py          # LangGraph state machine
│   ├── use_case.py        # Use cases (бизнес-логика)
│   ├── models.py          # Domain models (TypedDict)
│   └── prompts.py         # Промпты для всех агентов
├── config/                 # Конфигурация
│   ├── settings.py        # Pydantic Settings (.env)
│   └── logging_config.py  # Настройка логирования
├── storages/               # Хранилища
│   └── session_storage.py # In-memory session management
├── system/                 # Системные утилиты
│   └── exceptions/        # Exception handlers
├── utils/                  # Утилиты
│   └── logger.py          # Структурированное логирование
└── templates/             # UI
    └── index.html         # Apple-style интерфейс
```

## 🤖 Архитектура агентов

### Orchestrator
Собирает начальную информацию о кандидате: имя, уровень (junior/middle/senior), опыт, позицию.

### Expert Pool (параллельное выполнение)
- **Python Specialist**: Оценивает техническую точность ответов (1-10)
- **Fact Checker**: Обнаруживает галлюцинации и технические неточности
- **HR Agent**: Оценивает soft skills и коммуникацию

### SGR Cycle (Sequential)
- **Observer/Mentor**: Анализирует данные экспертов, создает скрытые инструкции, анализирует sentiment
- **Interviewer**: Формулирует ответ на основе инструкций, адаптирует сложность
- **Validator**: Проверяет качество ответа (loop если не проходит валидацию)

### Finalizer
Генерирует финальный отчет с:
- Decision (grade, hiring recommendation, confidence score)
- Skills Matrix (с confirmed_skills)
- Knowledge Gaps (с educational_content для каждого пробела)
- Roadmap развития

## 📡 API Endpoints

### WebSocket
```
ws://localhost:8000/api/v1/interview/ws
```

**Сообщения:**
- `{"action": "start", "session_id": "..."}` - Начать интервью
- `{"action": "message", "session_id": "...", "message": "..."}` - Отправить сообщение
- `{"action": "get_state", "session_id": "..."}` - Получить состояние

### REST API

#### Download Interview Log
```
GET /api/v1/interview/download-log?session_id=<session_id>
```

Возвращает JSON файл с полным логом интервью в формате TZ:
- `participant_name`
- `turns` (массив ходов с `turn_id`, `user_message`, `agent_visible_message`, `internal_thoughts`)
- `final_feedback` (JSON строка с финальным отчетом)

## 🎨 Особенности UI

- **Apple-Style Design**: Минимализм, glassmorphism, плавные анимации
- **Real-time Updates**: WebSocket для мгновенных обновлений
- **Internal Thoughts Panel**: Видимость "внутренних мыслей" системы
- **Metrics Dashboard**: Отслеживание токенов, latency, валидаций
- **Loading States**: Плавные индикаторы загрузки
- **Final Report Modal**: Красивое отображение финального отчета

## 📝 Логирование

Система создает структурированные логи в формате JSON:

```
logs/
└── interview_log_YYYYMMDD_HHMMSS.json
```

Каждый лог содержит:
- `participant_name`
- `turns` - все ходы интервью
- `final_feedback` - финальный отчет
- `events` - все события системы
- `metrics` - метрики производительности

## 🔒 Безопасность

- ✅ API ключи хранятся в `.env` (не коммитятся в git)
- ✅ Непривилегированный пользователь в Docker
- ✅ CORS настроен для production
- ✅ Валидация всех входных данных через Pydantic

## 🚀 Деплой на VPS

Подробная инструкция в [DEPLOY.md](./DEPLOY.md)

Кратко:
```bash
git clone <repo> && cd node-hr
cp env.example .env
nano .env  # Добавьте MISTRAL_API_KEY
docker-compose up -d --build
```

## 🧪 Разработка

### Структура кода

- **Без комментариев**: Код самодокументируемый
- **Type Hints**: Полная типизация
- **Dependency Injection**: Через `deps.py`
- **Use Case Pattern**: Бизнес-логика изолирована
- **Clean Architecture**: Четкое разделение слоев

### Best Practices

- ✅ Разделение на слои (API → Core → Storage)
- ✅ Dependency Injection через deps.py
- ✅ Use Case паттерн для бизнес-логики
- ✅ Pydantic Settings для конфигурации
- ✅ Exception handlers для централизованной обработки ошибок
- ✅ Lifespan для startup/shutdown логики
- ✅ Структурированное логирование с цветами
- ✅ Async/await для всех I/O операций

## 📊 Метрики

Система отслеживает:
- **Tokens**: prompt_tokens, completion_tokens, total_tokens
- **Latency**: средняя задержка ответов LLM
- **Validations**: количество попыток валидации
- **Sentiment**: анализ настроения кандидата

## 🐛 Troubleshooting

### Проблема: "MISTRAL_API_KEY not found"
**Решение**: Убедитесь, что файл `.env` существует и содержит `MISTRAL_API_KEY`

### Проблема: Docker не запускается
**Решение**: 
```bash
docker-compose down
docker-compose up -d --build
```

### Проблема: WebSocket не подключается
**Решение**: Проверьте, что порт 8000 открыт и не занят другим процессом

### Проблема: Отчет не генерируется
**Решение**: Проверьте логи `docker-compose logs -f nodehr` и убедитесь, что API ключ валидный

## 📄 Лицензия

Проект создан для образовательных целей.

## 👥 Авторы

Разработано с использованием best practices из ASFP-проектов.

---

**NodeHR** - Премиум Multi-Agent Interview Coach 🤖✨
